# HANDOFF — (empty)
Milestone: 1 — Combat slice **CLOSED** 2026-08-13. Milestone 2 not started.

M1's working state was archived into its close commit and this file was emptied per
`/closeout`'s milestone-completion path. Nothing here is pending.

Start the next session with `/kickoff 2`. Everything M2 needs is already durable:
- **CLAUDE.md** — M2 scope; **GAME-RULES §5** — M2 exit criteria (the gate to design against).
- **ROADMAP P29** — the agreed next build: enemy action repertoire / distance-conditioned
  action selection, ranged action as first second-action consumer, P17 pulled forward.
  Carries the composition fence.
- **BRAIN.md** — accumulated wisdom, including the four lessons M1's final batch produced.
- Carried-open items are listed in the M1 close commit (wand EXPLOIT still provisional,
  5.10's narrow chain-flinch feel, P28's animation revalidation trigger, the three
  GAME-RULES §3 rules needing a human edit, and P14's working-title question).

## Post-M1 work landed
**P16 shield bump + perfect parry** (Treat, taken at M1 close). BUMP is a short authored
SLIDE (candidate B: 2.5 over 10 ticks), perfect parry marks the attacker PARRY EXPOSED.

## Open items
- **P16 BUMP behavioural consequence UNTESTED:** B's 10-tick slide spans most of Fang's
  12-tick windup, so bumping a winding-up Fang can carry it out of reach and make the
  attack whiff. Legitimate spacing payoff once; if it ever becomes the COMMON case,
  bump has become de facto attack denial and is stealing perfect parry's mastery role.
  **Candidate A (2.0 / 7) is the pre-identified one-edit fallback.** B was validated for
  MOTION/READABILITY only ("reads sufficiently like a shove/slide"), never for this.
  No further P16 bump tuning without a specific playtest finding.
- **BUMP pass-through (ROADMAP P20):** a bumped enemy can slide through another enemy
  and past the visual arena edge — neither is an authoritative blocker today.
  Revalidate when body-blocking or world bounds become real.
- Carried from M1 close: wand EXPLOIT still PROVISIONAL · 5.10's narrow chain-flinch
  feel unvalidated · P28's animation/weapon-visual revalidation trigger · three
  GAME-RULES §3 rules needing a human edit · P14 working title (itch slug now permanent).

Public M1 build: https://bmyers32.itch.io/knight-depths
