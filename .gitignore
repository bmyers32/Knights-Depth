# Godot 4+ specific ignores
.godot/
/android/

# Export output — disposable build artifacts, never tracked (repo tracks no release binaries)
/build/

# Python bytecode from scripts/ (guard.py + its adversarial tests)
__pycache__/
