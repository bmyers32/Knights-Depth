class_name FailureOverlay
extends CanvasLayer
## Temporary M1 run-end scaffolding (Phase D step 8, Phase 3) — a plain text overlay,
## nothing more. The real Emergency Recall/extraction screen (WORLD-CANON framing) is
## explicit M2 scope (GAME-RULES §5); do not build toward it here.

func show_failure() -> void:
	visible = true
