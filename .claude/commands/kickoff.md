---
description: Start a session on a milestone (fresh context)
argument-hint: <milestone number>
---
Fresh session on Milestone $ARGUMENTS.
1. Read HANDOFF.md and the Milestone $ARGUMENTS row in CLAUDE.md + its GAME-RULES §5
   exit criteria. (CLAUDE.md/AGENTS.md/GAME-RULES.md are already loaded — do not re-read.)
2. Run /recon scoped to only what this milestone touches — read-only.
3. State verified topology per AGENTS.md 4 Invariables: state ownership, feedback,
   blast radius (especially: does this constrain M3 netcode?), timing.
4. Plan as a short prose walkthrough (input → command → sim → event → screen where
   relevant) + files to create/modify. Flag assumptions and Medium+ ambiguity per the
   Entry Protocol. Note any concept the user hasn't met yet and offer
   "I build" vs "you drive, I review" for it.
5. WAIT for approval before writing any code.
The milestone is done only when /closeout confirms ALL §5 exit criteria.
